# Database configuration
DB_DRIVER=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=php-starter
DB_USER=root
DB_PASS=root

# Redis configuration
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Security
# Comma-separated list of trusted proxy IPs, or * to trust all (if behind a WAF)
TRUSTED_PROXIES=
